<h1>Welcome To Identity Server</h1>
@if (User.Identity.IsAuthenticated)
{
    <p>User is LoggedIn</p>
    @foreach (var claim in User.Claims)
    {
        <dt>@claim.Type</dt>
        <dd>@claim.Value</dd>
    }
}
else
{
    <p>User is not LoggedIn</p>
}